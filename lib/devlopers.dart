import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class devlop extends StatelessWidget {
   devlop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:
      Center(child: Text("@ by mahmoud hany elzeiny",
      style: TextStyle(
        fontSize: 20,

      ),)),
    );
  }
}
